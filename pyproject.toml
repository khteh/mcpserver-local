[project]
name = "mcpserver-local"
version = "1.0.0"
description = "Local MCP Server"
authors = [
    { name = "Kok How, Teh", email = "funcoolgeek@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "mcp"
]

[dependency-groups]
dev = [
    "pytest",
]